---

---

<section id="artists" class="artist-spotlight" aria-label="Artista Destacado">
  <div class="noise"></div>
  <div class="content-wrapper">
    <article class="artist-info">
      <div class="label-wrapper">
        <span class="label">RESIDENT ARTIST</span>
        <div class="line"></div>
      </div>

      <h2 class="artist-name">ANAHI</h2>

      <div class="specs">
        <div class="spec-item">
          <span class="spec-label">ESTILO</span>
          <span class="spec-value">FINE LINE &bull; MINIMALISMO</span>
        </div>
        <div class="spec-item">
          <!-- <span class="spec-label">EXPERIENCIA</span>
          <span class="spec-value">5 AÑOS</span> -->
        </div>
      </div>

      <p class="bio">
        Especialista en la delicadeza del trazo y la composición etérea. Su
        trabajo explora la intersección entre la elegancia minimalista y la
        oscuridad sutil, creando piezas que fluyen naturalmente con la anatomía.
      </p>

      <div class="cta-wrapper">
        <a
          href="#contact"
          class="cta-button"
          aria-label="Agendar cita con Anahi"
        >
          <span class="btn-text">AGENDAR CITA</span>
          <span class="btn-bg"></span>
        </a>
      </div>
    </article>

    <div class="artist-visual" aria-hidden="true">
      <div class="image-container">
        <img
          src="/artist/anahi.jpg"
          alt="Anahí - Artista de tatuajes especialista en Fine Line y Minimalismo"
          class="artist-image"
          loading="lazy"
        />
        <div class="image-overlay"></div>
      </div>
      <div class="decorative-circle"></div>
    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const section = document.querySelector(".artist-spotlight");
  const name = document.querySelector(".artist-name");
  const infoElements = document.querySelectorAll(
    ".label-wrapper, .specs, .bio, .cta-wrapper"
  );
  const image = document.querySelector(".image-container");
  const circle = document.querySelector(".decorative-circle");

  const tl = gsap.timeline({
    scrollTrigger: {
      trigger: section,
      start: "top 60%",
      end: "bottom bottom",
      toggleActions: "play none none reverse",
    },
  });

  tl.from(name, {
    x: -100,
    opacity: 0,
    duration: 1.2,
    ease: "power4.out",
  })
    .from(
      infoElements,
      {
        y: 30,
        opacity: 0,
        duration: 0.8,
        stagger: 0.1,
        ease: "power3.out",
      },
      "-=0.8"
    )
    .from(
      image,
      {
        scale: 0.9,
        opacity: 0,
        duration: 1.5,
        ease: "power2.out",
      },
      "-=1"
    )
    .from(
      circle,
      {
        scale: 0,
        opacity: 0,
        duration: 1,
        ease: "elastic.out(1, 0.5)",
      },
      "-=1.2"
    );

  // Parallax for image
  gsap.to(image, {
    y: 50,
    scrollTrigger: {
      trigger: section,
      start: "top bottom",
      end: "bottom top",
      scrub: 1,
    },
  });
</script>

<style>
  .artist-spotlight {
    background-color: var(--color-black);
    color: var(--color-white);
    min-height: 100vh;
    position: relative;
    display: flex;
    align-items: center;
    padding: 6rem 2rem;
    overflow: hidden;
    border-top: 1px solid #222;
  }

  .noise {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1;
    opacity: 0.4;
  }

  .content-wrapper {
    max-width: 1400px;
    width: 100%;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    position: relative;
    z-index: 2;
    align-items: center;
  }

  .artist-info {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .label-wrapper {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .label {
    font-family: var(--font-body);
    font-size: 0.9rem;
    letter-spacing: 0.2em;
    color: #666;
    text-transform: uppercase;
  }

  .line {
    height: 1px;
    width: 60px;
    background-color: #333;
  }

  .artist-name {
    font-family: var(--font-display);
    font-size: 12vw;
    line-height: 0.8;
    margin: 0;
    color: var(--color-white);
    letter-spacing: -0.05em;
    position: relative;
    z-index: 10;
    mix-blend-mode: difference;
  }

  .specs {
    display: flex;
    gap: 3rem;
    margin-top: 1rem;
    border-top: 1px solid #222;
    border-bottom: 1px solid #222;
    padding: 1.5rem 0;
  }

  .spec-item {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .spec-label {
    font-family: var(--font-body);
    font-size: 0.75rem;
    color: #666;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .spec-value {
    font-family: var(--font-display);
    font-size: 1.1rem;
    color: var(--color-white);
  }

  .bio {
    font-family: var(--font-body);
    font-size: 1.1rem;
    line-height: 1.6;
    color: #999;
    max-width: 500px;
  }

  .cta-button {
    display: inline-block;
    position: relative;
    padding: 1rem 2.5rem;
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-white);
    background: transparent;
    overflow: hidden;
    border: 1px solid var(--color-white);
    transition: color 0.3s ease;
    margin-top: 1rem;
    text-decoration: none;
    text-transform: uppercase;
    width: fit-content;
  }

  .btn-text {
    position: relative;
    z-index: 2;
  }

  .btn-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--color-white);
    transform: translateX(-100%);
    transition: transform 0.3s ease;
    z-index: 1;
  }

  .cta-button:hover {
    color: var(--color-black);
  }

  .cta-button:hover .btn-bg {
    transform: translateX(0);
  }

  .artist-visual {
    position: relative;
    height: 100%;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .image-container {
    width: 100%;
    height: 100%;
    position: relative;
    overflow: hidden;
    filter: grayscale(100%) contrast(1.1);
    transition: filter 0.5s ease;
  }

  .image-container:hover {
    filter: grayscale(0%) contrast(1);
  }

  .artist-image {
    width: 100%;
    height: 100%;
    min-height: 700px;
    object-fit: cover;
    object-position: top center;
  }

  .image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.8), transparent);
    pointer-events: none;
  }

  .decorative-circle {
    position: absolute;
    top: 50%;
    right: -10%;
    transform: translateY(-50%);
    width: 400px;
    height: 400px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    z-index: -1;
    pointer-events: none;
  }

  @media (max-width: 968px) {
    .content-wrapper {
      grid-template-columns: 1fr;
      gap: 4rem;
    }

    .artist-name {
      font-size: 18vw;
    }

    .artist-visual {
      min-height: 400px;
      order: -1; /* Image first on mobile? Or keep text first? Let's keep text first for context */
    }

    .artist-image {
      min-height: 400px;
    }
  }
</style>
